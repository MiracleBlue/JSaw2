<h3>{{name}}</h3>
<button class="playback-toggle">
	{{#if playing}}
		Stop
	{{else}}
		Play
	{{/if}}
</button>
<table>
<thead>
  <tr>
    <td></td>
    {{#times steps}}
      <td>step</td>
    {{/times}}
  </tr>
</thead>
<tbody>
</tbody>
</table>